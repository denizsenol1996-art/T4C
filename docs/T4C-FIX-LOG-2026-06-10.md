# Transfer4cars — Fix-log 2026-06-10

> **Doel:** elke wijziging hier loggen. Backup-naam + verify-stap zichtbaar.
> **Backup-conventie:** `<file>.bak-<FIXCODE>-<YYYYMMDD>`
> **Verify-conventie:** `curl http://127.0.0.1:3000/<path> -H "Host: transfer4cars.com" | grep <marker>`
> **Cross-link:** `T4C-MAP-COMPLEET-2026-06-10.md` + `T4C-INFOARCHITECTUUR-2026-06-10.md`

---

## Done

### NL1 — "+ Nieuwe veiling"-knop op `/veilingen/` + auto-open form ✅
- **Bron:** vorige sessie crash 06-10 13:29 vlak voor save (CRASH-LOG entry)
- **Files (2):**
  - `/opt/t4c/sites/transfer4cars/veilingen/index.html` (regel 393–395): staff-versie van user-area krijgt groene CTA-knop `+ Nieuwe veiling` → `/admin/#nieuw`
  - `/opt/t4c/sites/transfer4cars/admin/index.html` (`init()`): `if (location.hash === "#nieuw") toggleForm(true);`
- **Backups:** `*.bak-NL1-20260610`
- **Verify:**
  - `curl /veilingen/ | grep "Nieuwe veiling"` → match
  - `curl /admin/ | grep "location.hash"` → match
- **PM2-restart:** niet nodig (static HTML, express.static serveert direct)

---

### Groep A — Admin-nav consistentie (NL2 + NL3 + NL5) ✅
- 4 admin-pages dezelfde canonieke nav (zie INFOARCHITECTUUR §2): `Veilingen | Inbox | Transport | Analytics | Taxatie-rapporten | Server-monitor | Site`
- Weggehaald: "Klassiek", "ATX Pipeline", "CarDatax"
- Files: `/admin/index.html`, `/admin/inbox/index.html`, `/admin/transport/index.html`, `/admin/analytics/index.html`
- Backups: `*.bak-NavA-20260610`
- **Verify:** `curl /admin/...` × 4 → alle 4 tonen identieke nieuwe links. `/telex-inkoop/` + `/admin/atx/` → 200.
- **PM2-restart:** niet nodig (static HTML)

### ATXLOGIN — "Tussen-login" uit ATX-pipeline (root cause: redirect ging naar PWA) ✅
- **Diagnose:** atx-admin server.js (port 3110) heeft GEEN eigen login-middleware. De "tussen-login" was een FE-side bug in `/opt/atx-pipeline/public/inbound.js` + `inbound-detail.js`:
  - `LOGIN_URL = '/app/?next=...'` → wees naar CarDataX-PWA i.p.v. T4C `/login/`
  - `alert(reason + 'Je wordt nu doorgestuurd…')` + auto-redirect bij ontbrekend `localStorage.t4c_token`
  - Boot-time check (regel 17/21) blokkeerde page-render zelfs als T4C-token er was maar nog niet opgehaald
- **Fix:** beide files (`inbound.js` + `inbound-detail.js`) `goToLogin()` herschreven → toont nu inline banner bovenaan pagina met klikbare "Inloggen"-knop naar `/login/?next=...`. Boot-time `if (!IS_LOCAL && !TOKEN)` redirect-block weg. AUTH_HEADERS + Bearer-token-flow intact.
- **Effect:** klik vanuit T4C-admin nav "Taxatie-rapporten" → token zit al in localStorage (T4C-login) → page laadt direct, geen banner, geen tussen-login. Bij expired session: vriendelijke banner, geen alert.
- **Files (2):** `inbound.js`, `inbound-detail.js`
- **Backups:** `*.bak-ATXLOGIN-20260610`
- **Verify:** `curl /inbound.js` zowel direct (3110) als via T4C-proxy (3000) toont nieuwe code; `node -c` op beide = syntax OK; geen `/app/` redirect-string meer
- **PM2-restart:** niet nodig (statics via express.static)
- **Open vervolg:** 95 atx-admin restarts (5u uptime) apart onderzoeken; admin-dashboard 3200 (`T4C-admin-2026`) is APARTE service, blijft eigen login houden (= SEC-2 beslis-vraag)

### ATX404 — Rapport-link 404 op `/admin/inbound/:id` ✅
- **Symptoom:** `https://transfer4cars.com/admin/inbound/92` (en alle andere ID's) gaf 404 "Pagina niet gevonden".
- **Root cause:** Twee URL-paden voor ATX-detail:
  - **Publieke route via T4C-proxy:** `/telex-inkoop/:id` (server.js:495-497 rewrite → `/admin/inbound/:id` interne ATX-call)
  - **Interne ATX-route:** `/admin/inbound/:id` (op port 3110, niet rechtstreeks via T4C exposed)
  - T4C skip-list (server.js:318) bevat alleen `/telex-inkoop` → `/admin/inbound/*` valt door naar static-handler → 404.
  - FE bouwde link met `<a href="/admin/inbound/${r.id}">` (regel 111) + `window.location.href = '/admin/inbound/${id}'` (regel 184) → externe URL klopte niet.
- **Fix:** beide FE-links in `inbound.js` veranderd naar `/telex-inkoop/${id}` (correcte publieke URL).
- **Files (1):** `inbound.js`
- **Backups:** `*.bak-ATX404-20260610`
- **Verify:**
  - `curl /admin/inbound/92` (via T4C) → 404 (expected, niemand linkt er nog naartoe)
  - `curl /telex-inkoop/92` (via T4C) → 200, md5 identiek aan ATX-direct
  - `curl /inbound.js` toont nieuwe `/telex-inkoop/` href, geen `/admin/inbound` meer
  - `node -c` syntax OK
- **PM2-restart:** niet nodig (static JS)
- **Open vervolg:** schade-render check op specifiek rapport (rapport 92 nu bereikbaar — user gaat testen)

---

### CLEANUP-1 — 11 backup-files uit transfer4cars-tree ✅
- Verplaatst naar `/opt/t4c/backups/cleanup-20260610/` (al bestaande dir, nu 15 files totaal)
- Bestanden: alle `*.pre-dna-2026060[9]*` + `*.self-service-discarded-*` uit `aanmelden/`, `transport/`, `verkoop/`, `login/`, `auto/`, `account/`, `aanbod/`
- Naming-conventie: `<dir>.<orig-filename>` om collisions te voorkomen
- **Verify:** `find ... -name "*.pre-dna-*"` → 0 hits in tree

---

### SEC-1 — 2 admin-endpoints zonder auth ✅
- `/api/admin/bulk-seed-queue` + `/api/admin/cleanup-db` kregen `authMiddleware, adminOnly`
- File: `/opt/t4c/backend/routes/admin.js` (regel 454 + 507)
- Backup: `admin.js.bak-SEC1-20260610`
- **Verify:** zonder token = 401 (beide), met admin-JWT = 200 (bulk-seed; cleanup niet getest om data te sparen, middleware-pad identiek)
- **PM2-restart:** `t4c-server` (nieuwe pid 1743123)

---

### SEC-2 — helmet + CORS-whitelist (zero-risk security-hardening) ✅ (22:55)
- **Bron:** AUDIT-TRANSFER4CARS-2026-06-10 BLOCKERS #1-3 (helmet, CORS, .env permissions)
- **Acties:**
  1. `chmod 600 /opt/t4c/.env /opt/t4c/backend/.env` (was 664) — JWT_SECRET/SMTP_PASS/DV_WEBHOOK_PASS niet meer leesbaar door other-users
  2. `npm install helmet --save` (`/opt/t4c/package.json`)
  3. `server.js`: import helmet + middleware (HSTS 1jr + preload, X-Frame SAMEORIGIN, X-Content-Type-Options nosniff, Referrer-Policy no-referrer, COOP same-origin). CSP staat UIT (inline-scripts/styles risico — apart traject).
  4. `server.js`: CORS open-wildcard → origin-callback met `ALLOWED_ORIGINS` set (transfer4cars.com + www + cardatax.com + www). `cb(null, false)` op vreemd-origin (geen 500-spam).
- **Files (1 code + 2 perms):** `server.js`, `.env` × 2
- **Backups:** `/opt/t4c/backups/pre-blockers-20260610-2215/` (sha256-verified identiek aan origineel vóór edit)
- **Verify:**
  - `node -c server.js` → OK
  - `pm2 reload t4c-server` → uptime 12s, 0 errors
  - 11/11 publieke routes → 200 (curl test)
  - `Origin: https://evil.com` → response zonder ACAO-header (browser blokkeert)
  - `Origin: https://transfer4cars.com` → ACAO + credentials true
  - `curl -sI` toont alle helmet-headers actief
- **PM2-restart:** `pm2 reload t4c-server` × 2 (eerste reload had `cb(new Error)` → 500-spam, fix `cb(null, false)` → schoon)

### SEO1 — Meta-fix `/index.html` (titel + desc + apple-touch-icon) ✅ (23:00)
- **Bron:** Seobility-rapport door user gedeeld — title 706→<580px, desc 1151→<1000px, apple-touch-icon ontbreekt
- **Files (1):** `/opt/t4c/sites/transfer4cars/index.html`
- **Backup:** `index.html.bak-SEO1-20260610` (sha256-verified)
- **Changes:**
  - Title: 75→57 chars: "Transfer4Cars · B2B Autoveilingen & Occasions | Langeraar"
  - Meta-desc: 186→139 chars: "Transfer4Cars Langeraar: B2B-veilingen, kwaliteits-occasions en EU-import voor handelaren. Voorraad direct online, transport door NL en EU."
  - Apple-touch-icon: `<link rel="apple-touch-icon" href="/m/apple-touch-icon.png">` (`/m/` reverse-proxy naar `/opt/t4c/sites/cardatax/m/` shared assets)
- **Verify:**
  - `curl https://transfer4cars.com/` toont nieuwe title + apple-touch-icon link
  - `https://transfer4cars.com/m/apple-touch-icon.png` → HTTP 200 image/png
  - sitemap.xml + robots.txt still 200
- **PM2-restart:** niet nodig (static HTML)
- **Open vervolg:** content-uitbreiding 453→800+ woorden, 30 headings dedupliceren, Langeraar-vs-Alphen-aan-den-Rijn-consistency-vraag (Schema.org-adres = Alphen, title/desc = Langeraar)

### CF-REDIR — Cloudflare www→non-www + HTTPS redirect handleiding ✅ (23:05)
- **Doc:** `/opt/t4c/docs/CF-WWW-REDIRECT-2026-06-10.md` (5-stappen-handleiding voor user)
- **Niet uitgevoerd** — vereist user-actie in CF dashboard (geen API-token in Claude env)
- **Impact bij user-uitvoering:** Seobility Server-config 0%→100%, critical SEO-blocker weg

### QW-MEM — max_memory_restart 1500MB op t4c-server ✅ (23:30)
- **Bron:** AUDIT BELANGRIJK #17; voorkomt OOM-zombie cascade
- **File:** `ecosystem.config.js` (+ pm2 save voor resurrect)
- **Backup:** `pre-quickwins2-20260610-2305/ecosystem.config.js`
- **Verify:** `pm2 jlist` → `max_memory_restart=1572864000` (1500MB)
- **PM2:** `pm2 reload ecosystem.config.js --update-env` + `pm2 save`

### QW-WATCHDOG — Watchdog smoke-test ✅ (23:32)
- **Bron:** AUDIT infra-check; watchdog-log laatste entry 2026-06-09 19:36 leek inactief
- **Test:** `bash -x /opt/t4c/watchdog.sh` toont flock+curl+health-check+exit-0 schoon. Parallel-run: tweede blocked door flock.
- **Conclusie:** watchdog werkt correct. Geen log-entries = ontwerp (alleen bij fail loggen). Open verbetering uit STABILITEIT-PROTOCOL §B3: "tick"-log toevoegen zodat zichtbaar is dat hij leeft.

### GA4-CLEAN — Stale GA4-comments uit /aanmelden/ontvangen/ ✅ (23:33)
- **Bron:** AUDIT Sectie A — `XXXXXXX` placeholders in comments; ID was eigenlijk al ingevuld
- **File:** `/opt/t4c/sites/transfer4cars/aanmelden/ontvangen/index.html` regel 20-22 + regel 30
- **Backup:** `pre-quickwins2-20260610-2305/ontvangen-index.html`
- **Note:** `/index.html` HAD AL GA4 — eerste Explore-agent had stale info. Geen edit nodig op homepage.
- **Open:** Google Ads `AW-CONVERSION_ID/conv_label` blijft TODO — vereist Jurgen's Ads-account-ID.

### SEC-3 — Rate-limit op /bid + /watch + /transport/afrekening/betaal ✅ (23:40)
- **Bron:** AUDIT BELANGRIJK #13; bid-flooding preventie
- **File:** `/opt/t4c/backend/routes/veilingen.js` — nieuwe `rateLimit(maxPerMin)` factory + cleanup-interval 5min
- **Limits:**
  - `/api/veiling/:id/bied` → 30/min/user (fast-bid-war OK, scripted-flood geblokkeerd)
  - `/api/veiling/watch` → 60/min/user
  - `/api/veiling/:id/transport`, `/afrekening`, `/betaal` → 10/min/user
- **Backup:** `pre-quickwins2-20260610-2305/veilingen.js`
- **Verify:** `node -c` OK, `pm2 reload` OK, 5/5 publieke routes 200, login-rate-limit blijft werkend (separaat)
- **Open:** Staff/admin POST/PUT routes hebben geen per-user rate-limit (acceptable — trusted users)

### SEC-4 — Logout JWT-blacklist ✅ (23:48)
- **Bron:** AUDIT BLOCKER #5 (afgezwakt: simpele blacklist tot Redis/DB-backed)
- **Files (2):** `/opt/t4c/backend/lib/auth.js` + `/opt/t4c/backend/server.js`
- **Implementatie:**
  - `lib/auth.js`: `_revokedTokens` Set + `revokeToken()` + `isRevoked()` + cleanup-interval 6u (drop verlopen tokens via jwt.decode().exp)
  - `authMiddleware` checkt `isRevoked(token)` vóór `jwt.verify`
  - `app.get('/api/me')` ook isRevoked-check
  - Nieuwe endpoint `POST /api/logout` → revokeToken + audit-log
- **Backups:** `pre-quickwins2-20260610-2305/lib-auth.js`, `pre-blockers-20260610-2215/server.js` (server.js verder geëdit, opnieuw backuppen aanbevolen)
- **Verify:** node -c OK, pm2 reload OK, `POST /api/logout` zonder token → 401, met (fake) token → 200, 3/3 publieke routes 200
- **Beperking (gedocumenteerd):** in-memory Set reset bij pm2 restart → revocaties verloren na restart. Frontend `localStorage.clear()` blijft primaire bescherming. Upgrade naar Redis/DB-backed = aparte taak.

### SEC-INV — Plaintext-password inventarisatie (BLOCKER #4 verificatie) ✅ (00:05)
- **Bron:** AUDIT BLOCKER #4 — plaintext-upgrade-pad in `db.js:1247` zou risico zijn als users daadwerkelijk plaintext hadden
- **Read-only scan:** `sqlite3 /opt/t4c/data/t4c.db` query op `users` tabel
- **Resultaat:** `totaal=4, bcrypt=4, leeg=0, plaintext=0` → **geen huidige exposure**
- **Conclusie:** alle 4 actieve accounts (2 admin + 1 t4c + 1 inkoper, per FOUNDATION-AUDIT) hebben `$2a$/$2b$`-hash. Code-fallback in `db.js:1243-1252` blijft als defense-in-depth voor edge-cases (directe DB-insert), maar live risico is 0.
- **Open vervolg:** bij elke nieuwe user-registratie via API is hash gegarandeerd (`bcryptjs.hashSync(pw, 10)` in `routes/misc.js:63` + `admin.js:203`). Wel toevoegen: startup-warning als users-tabel non-bcrypt rijen bevat (defensieve log).
- **Geen DB-mutatie nodig vanavond.** §A0 backup-DB-eerst skip → was read-only scan.

### LOGIN-500-FIX — /api/login crashte met 500 bij missende body ✅ (06:35)
- **Bron:** full sanity-check 2026-06-11 vond pre-existing bug
- **Symptoom:** POST /api/login zonder body of zonder username/password → Internal Server Error 500 (HTML, niet JSON). Bots/scanners spammen logs.
- **Root cause:** `verifyUser(undefined, undefined)` crashte in db.js, geen try/catch op login handler, Express default error → 500
- **Fix in `server.js` /api/login:**
  - Try/catch om hele handler (defensive)
  - Early-validate: `if (!username || !password || typeof !== "string") return 400 "Gebruikersnaam en wachtwoord vereist"`
  - 500-fallback logged + cleane JSON response naar client
- **Verify 5 edge-cases:**
  - No body → 400 ✓
  - `{}` → 400 ✓
  - Alleen username → 400 ✓
  - Fake creds → 401 ✓
  - Cross-origin POST → 403 (Origin-check) ✓
- **Backup:** `server.js.before-login-500-fix`
- **Open follow-up:** ander endpoints met body-input controleren op same pattern (`/api/register`, etc al beschermd via try/catch ✓)

### ATX-DIAG — /admin/atx/ leeg dashboard diagnose (server OK, browser-cache user) ✅ (06:10)
- **User-report:** "Admin Dashboard, Overzicht, Processen, Databases, Scrapers, Pipeline … dit is een admin dashboard die is leeg deze doet niks"
- **Diagnose:** server-side flow VOLLEDIG werkend bevestigd. Met geldig admin-JWT (gesigned met DB-secret): dashboard HTML 9720B + alle 5 API endpoints (server/pm2/scrapers/pipeline/db/list) → 200 JSON met data.
- **Mijn aanvankelijke verwarring:** JWT-test gebruikte `.env` JWT_SECRET (64 chars), terwijl t4c-server `settings.jwt_secret` uit DB gebruikt (96 chars, totaal andere waarde). "invalid signature" log-entries leek bug maar was test-fout.
- **Conclusie:** user moet HARD REFRESH (`Ctrl+Shift+R`) op /admin/atx/. Browser-cache van eerder ATXGATE-broken-state zit nog. Of clear cookies/sessionStorage en re-login.
- **Memory toegevoegd:** `feedback_t4c_jwt_secret_location.md` — voor toekomstige JWT-tests altijd DB-secret gebruiken
- **Geen code-fix nodig** — alleen tijdelijk debug-log toegevoegd + verwijderd (verify-only)
- **Geen .env JWT_SECRET-cleanup gedaan** — open issue: .env values zijn dead code, overweg later op te ruimen

### NAV-DEFER-FIX — Bug-fix op NAV-UNIFY: defer-race deed inline-script crashen ✅ (05:00)
- **User-report:** "ik kom weer via veilen op dezelfde bullshit terecht — letterlijk no difference"
- **Root cause:** mijn NAV-UNIFY-edit zette `<script src="/js/user-nav.js" defer></script>` in HEAD, maar inline-script in BODY roept `t4cUserNav(...)` direct aan. Per HTML-spec: `defer` wacht op parse-complete, inline runt tijdens parsing. → `ReferenceError: t4cUserNav is not defined` → inline-script crasht → user-area leeg + auth-content/guest-wall verkeerd → "zelfde bullshit" als voor de fix.
- **Fix 1:** verwijder `defer` op 5 pages (`/veilingen/`, `/veilingen/detail/`, `/aanbod/`, `/auto/`, `/account/`). user-nav.js wordt nu sync-loaded in HEAD (6.9KB, ms-snel).
- **Fix 2 (defensive):** `user-nav.js` self-init op DOMContentLoaded. Scant naar `#user-area`, render automatisch als inline-script niet voor handle. Voorkomt dat een toekomstige inline-crash de dropdown sloopt.
- **Lesson opgenomen in STABILITEIT-PROTOCOL §A3.10c**: script-load-order check verplicht voor externe JS met inline-callers.
- **Verify:** 5/5 pages live tonen sync (geen defer), user-nav.js self-load OK. Browser-execute-order gesimuleerd in node, t4cUserNav definition + inline call beide succesvol.
- **Mijn fout:** syntax-OK-tests gaven groen licht, maar dat valideerde alleen JS-parse, niet runtime-execution-order. Protocol §A3 was incompleet — nu §A3.10c toegevoegd.

### NAV-UNIFY — Unified user-dropdown + canonieke items op 6 klant-pages ✅ (04:25)
- **User-report:** "geen dropdown op andere pages dan homepage", "staff op /veilingen/ ziet geen Mijn account"
- **Audit:** 6 inconsistente issues gevonden (homepage-dropdown out-of-sync met admin-pages-nav, "ATX Pipeline" vs "Server-monitor", flat user-areas op 3 pages, staff geen mijn-account-link, CarDatax Platform-naam, missende items: Inbox/Analytics/Taxatie-rapporten in homepage-dropdown)
- **Canonieke menu's (single source of truth in /js/user-nav.js):**
  - Klant: Mijn account / Veilingen / Voorraad / Transport / Uitloggen
  - Staff: Veilingen-beheer / Inbox / Transport-planner / Analytics / Taxatie-rapporten / Server-monitor / CarDatax Pricing / (sep) / Mijn profiel / Naar publieke site / Uitloggen
- **User-correctie tijdens audit:** /app/ blijft in staff-dropdown — is CarDatax pricing-tool (memory T4C-INFOARCHITECTUUR was outdated)
- **Files (8 edits):**
  - **Nieuw:** `/opt/t4c/sites/transfer4cars/js/user-nav.js` (5950 chars) — `window.t4cUserNav(elementId, {currentPath, extraCta})`, role-aware, server-side logout-revoke via `/api/logout`, click-outside-close, active-state-highlight
  - **Wired:** /veilingen/index.html, /veilingen/detail/index.html, /aanbod/index.html, /auto/index.html, /account/index.html (5 pages — vervang inline `ua.innerHTML=...` blokken met `t4cUserNav(...)`)
  - **Items-list synced:** /index.html updateNav() — staff/klant items 1-op-1 match met user-nav.js
- **Backups:** `pre-nav-unify-20260611-0420/` (9 files: alle klant-pages)
- **Verify:** 6/6 pages JS-syntax OK (2/2 scripts elk), 6/6 pages live HTTPS 200, user-nav.js include zichtbaar in alle 6 HTML's
- **Niet aangeraakt:** /transport, /verkoop, /login, /aanmelden (geen user-area element). Admin-pages eigen horizontale nav blijft canoniek per NavA.
- **UX-winst:** staff op /veilingen/ heeft nu zowel "+ Nieuwe veiling" CTA losse groene knop ALS volledige admin-dropdown met "Mijn profiel" optie

### BTW-REQ — BTW-nummer required op B2B-aanmelding ✅ (04:35)
- **User-request:** "wij moeten ook het btw nummer hebben"
- **Schema-migration:** ALTER TABLE contact_requests ADD COLUMN btw TEXT DEFAULT '' (pm2 stop → ALTER → start). DB-backup vooraf: `pre-btw-migration-20260610-2249.db`
- **Backend (`routes/misc.js`):**
  - `/api/register`: `V.isValidBTW(d.btw) || !d.btw → 400 "BTW-nummer vereist"`
  - INSERT met btw kolom + bericht-template noemt BTW
- **Frontend (`/aanmelden/index.html`):**
  - Nieuw `<input id="btw" required pattern="NL\d{9}B\d{2}" maxlength="14">` met format-hint
  - JS body voegt btw toe + uppercase + strip-spaces
- **Backups:** `misc.js.before-G` (al uit eerder batch), backup form
- **E2E-test:** geen BTW → 400 ✓, invalid format → 400 ✓, valid NL864657079B01 → 200 ✓
- **Test-data cleanup:** pm2 stop → DELETE → start (volgens memory `feedback_t4c_sqljs_alter`)

### BUG-ACCOUNT — /account/ leeg door JS-syntax-error in eigen edit ✅ (03:25)
- **User-report:** "mijn account sectie is leeg" + frustratie
- **Root cause:** in E1-edit (watchlist+historie tabs) gebruikte ik `'<span style="font-family:\\'IBM Plex Mono\\',monospace">'` — dubbele-escape was te kort, JS-parser zag string-einde bij `\\'` → "Unexpected identifier 'IBM'" → Script #1 (14.5KB) crashte → tabs renderden niet → page leeg.
- **Fix:** `'<span style="font-family:var(--mono)">'` (CSS-variabele die op regel 19 al gedefinieerd is). 2 occurrences (watchlist + historie render).
- **File:** `/opt/t4c/sites/transfer4cars/account/index.html`
- **Verify:** `node Function(code)` parse-test op alle inline scripts: 2/2 OK. Live URL 200. data-tab="watchlist" + data-tab="historie" zichtbaar.
- **Lesson:** STABILITEIT-PROTOCOL §A3 uitgebreid met §A3.10b (inline-JS parse-test verplicht) + §A3.13b (vermijd template-escape, gebruik CSS-class/var).

### ATXGATE — /admin/atx/ wachtwoord-prompt vervangen door t4c-JWT-gate ✅ (03:40)
- **User-report:** "waarom zit hier nou toch een password tussen dan ik snap dit echt niet"
- **Root cause:** /admin/atx/ is proxy → admin-dashboard.js (pm2-proc, port 3200) die een eigen hardcoded `ADMIN_PASS='T4C-admin-2026'` had (regel 26 in `/opt/atx-pipeline/admin-dashboard.js`). Legacy uit pré-T4C-v2-tijd. Per T4C-MAP-COMPLEET-2026-06-10 §6 stond dit al gedocumenteerd als "twee inloggen", maar de fix was nog niet gedaan.
- **Fix:** `/opt/t4c/backend/server.js` — vóór proxy: t4c-JWT-gate die token uit cookie leest, verifieert role admin/staff/t4c. Bij ongeldig/missing: serveert mini-handshake-page die token uit `localStorage.t4c_token` naar cookie schrijft + reload. Anti-loop via `sessionStorage.t4c_atx_hs_tried`-flag (verlopen token → clear localStorage + login-redirect). Bij valid: injecteert `admin_auth=ok` cookie naar admin-dashboard → die's eigen check passeert automatisch.
- **File:** `/opt/t4c/backend/server.js` proxy-handler (regel 296-340)
- **Backup:** `pre-niet-acuut-20260611-0030/server.js.before-atx-gate`
- **Verify:** node -c OK, pm2 reload OK, /admin/atx/ → 200 (656B handshake-page met localStorage-check). Met admin-token in localStorage: handshake → cookie → reload → admin-dashboard dashboard direct zonder password.
- **Geen edit op admin-dashboard.js** — proxy-laag handelt alles af.
- **Lesson:** issues uit T4C-MAP-COMPLEET pro-actief fixen, niet wachten tot user erom vraagt.

### G1 — KvK + BTW + email + phone + postcode + password validators ✅ (02:15)
- **Bron:** AUDIT BELANGRIJK #20 (input-validation library); memory `feedback_cardatax_integrity_guardrail`
- **File:** `/opt/t4c/backend/lib/validators.js` (nieuw)
- **Helpers:** `isValidEmail`, `isValidKvK`, `isValidBTW`, `isValidPostcode`, `isValidPhone`, `isValidBedrijf`, `isStrongPassword`
- **KvK-checks:** 8 cijfers + anti-fake (12345678, all-zeros, all-same)
- **BTW-checks:** NL\d{9}B\d{2} format
- **Postcode:** NL 4cijfer+2letter, blokt SA/SD/SS (gereserveerd)
- **Password:** ≥8 chars, geen all-digit, geen common-list
- **Unit-tests:** 25/25 passed inline
- **Wired in:** `routes/misc.js` — `/api/register` + `/api/register/koper`
- **Backups:** `misc.js.before-G`
- **Cleanup:** test-row (Test BV / deniz-test-001@example.invalid) na E2E gewist via pm2 stop → sqlite3 DELETE → pm2 start (per memory `feedback_t4c_sqljs_alter`)
- **Verify:** 4/4 E2E test-cases correct (fake-KvK → 400, weak-pwd → 400, bad-email → 400, valid → 200)

### OS1 — Off-site backup script-template ✅ (02:20)
- **Bron:** AUDIT BELANGRIJK #16; STABILITEIT-PROTOCOL §B2 (off-site backup gap)
- **File:** `/opt/t4c/scripts/offsite-backup.sh` (nieuw, executable)
- **Werking:** GPG-encrypt nieuwste lokale backup → rclone push naar `remote:t4c-backups/<datum>/`, 90d remote-retention
- **Preflight-checks:** rclone aanwezig, remote geconfigureerd, GPG-recipient bestaat
- **Test-mode:** `--test` flag voor connectie-check zonder upload
- **Exit codes:** 0=OK 1=tool-missing 2=remote-not-configured 3=GPG-key-missing 4=test-fail 5=geen-lokale-backup 6=encrypt-fail 7=rclone-fail
- **User-setup nodig (~15 min):** `apt install rclone gpg`, `rclone config`, `gpg --gen-key`, test, crontab — gedocumenteerd in README
- **README:** `/opt/t4c/scripts/README.md` uitgebreid met setup-stappen

### E1 — Watchlist-tab + bidding-history in /account/ ✅ (02:40)
- **Bron:** AUDIT NICE-TO-HAVE #24
- **Backend:** `routes/veilingen.js`
  - `GET /api/mijn-veilingen` uitgebreid met `watchlist[]` (join veiling_watchers × veilingen) en `historie[]` (alle eigen biedingen met uitslag-flag)
  - Nieuwe `DELETE /api/veiling/watch/:id` (auth, user_id-ownership-check)
- **Frontend:** `sites/transfer4cars/account/index.html`
  - 2 nieuwe tabs: "Watchlist" + "Historie" (tussen Gewonnen en Facturen)
  - Watchlist-render met kenteken/eind/huidige_bod + "Bekijk" + "×" unwatch-knop
  - Historie-render met statusKlas/statusTxt per bod (gewonnen/verloren/live)
  - Nieuwe `unwatch(watchId, btn)` async helper met confirm + refresh
- **Backups:** `veilingen.js.before-E`, `account-index.html.before-E`
- **Verify:** /api/mijn-veilingen + DELETE-route 401 zonder token, HTML toont 2 nieuwe data-tab markers, 3/3 publieke routes 200

### F1 — Admin-UX polish: alert() → t4cToast() ✅ (02:55)
- **Bron:** AUDIT NICE-TO-HAVE #27
- **File:** `/opt/t4c/sites/transfer4cars/js/admin-toast.js` (nieuw, gedeeld)
- **Helper:** `window.t4cToast(msg, kind)` — kinds: `ok` (groen #00e68a) / `warn` (amber #ffb300) / `err` (rood #ef4444). Auto-dismiss 3.5-6s, klik-dismiss, slide-in animatie, max-w 380px right-top stacked
- **Bonus:** `window.t4cConfirm(msg)` Promise-based modal (vervangt klassieke `confirm()` voor delete-flows)
- **Wired in:** `admin/index.html` (4× alert → toast) + `admin/transport/index.html` (1× alert → toast). `admin/inbox/` en `admin/analytics/` hebben geen alert() calls.
- **Backups:** `admin-index.html.before-F`, `admin-transport-index.html.before-F`
- **Verify:** 0/5 alert() over alle admin-pages, 5/5 admin-pages + toast.js → 200
- **DNA:** CarDataX-tokens (Outfit-font + #00e68a + dark-glass) — consistent met overige UI

### H1 — Dead dirs verplaatst naar archive ✅ (01:15)
- **Bron:** AUDIT NICE-TO-HAVE #28; STABILITEIT-PROTOCOL §B2 disk-hygiene
- **Verplaatst (8 items, 1.1GB):** `backend.RECOVERY-FAILED`, `data/t4c.db.RECOVERY-FAILED`, `backend-test-*.ARCHIVE-*` × 3, `data-test-*.ARCHIVE-*` × 2, `watchdog.sh.bak-pre-redesign-20260609`
- **Doel:** `/opt/t4c/backups/cleanup-20260611/`
- **Verify:** server-health OK, 4/4 publieke routes 200, geen broken refs
- **Disk:** /opt 29G used (2%), geen impact

### I1 — /opt/t4c/scripts/README.md geschreven ✅ (01:20)
- **Bron:** STABILITEIT-PROTOCOL: scripts/ docs ontbraken
- **Inhoud:** doel + exit-codes + gebruik voor smoke-test.sh / restore-db.sh / db-snapshot.sh, recovery-procedures (4 scenarios met stappen)
- **Cross-link:** STABILITEIT-PROTOCOL §C (fall-back) + watchdog.sh

### A1 — Origin-check middleware (CSRF-equivalent) ✅ (01:30)
- **Bron:** AUDIT BELANGRIJK #19 (CSRF). Klassieke CSRF-token past niet bij JWT-Bearer-auth (geen auth-cookies = CSRF al grotendeels mitigated). Origin-check op POST/PUT/DELETE/PATCH biedt vergelijkbare defense-in-depth bovenop CORS.
- **File:** `/opt/t4c/backend/server.js` na CORS-middleware
- **Backup:** `pre-niet-acuut-20260611-0030/server.js.before-A`
- **Logic:**
  - GET/HEAD/OPTIONS → skip
  - Geen Origin én geen Referer → server-side caller (curl/mobile), OK
  - Origin in ALLOWED_ORIGINS → OK
  - Referer-host in ALLOWED_ORIGINS → OK
  - Anders → 403 "Origin niet toegestaan"
- **Verify (5 cases):**
  - POST Origin=transfer4cars.com → 401 (auth-fail = let door)
  - POST Origin=evil.com → 403 ✓
  - POST zonder Origin → 401 (let door)
  - GET Origin=evil.com → 200 (geen check op GET)
  - POST Referer=transfer4cars.com → 401 (let door op Referer-match)

### B1 — Factuur-PDF endpoint + frontend-knop ✅ (01:50)
- **Bron:** AUDIT BELANGRIJK #8 — gewonnen klant kon factuur niet downloaden
- **Backend:**
  - npm install pdfkit
  - GET `/api/factuur/:id/pdf` in `routes/veilingen.js` na `/api/factuur/:id`
  - Auth: koper-eigen OF role admin/t4c
  - Branded layout (groen accent #00cc7d, Helvetica fonts via pdfkit defaults — Outfit/Plex Mono vereist font-embed, latere upgrade)
  - 1 pagina A4: brand-header + factuur-meta + koper-block + voertuig-block + spec-tabel (bod/veilingkosten/transport) + subtotaal/BTW/totaal + betaalinstructie + footer
  - Marge-regeling (BTW=0) wordt herkend en vermeld
  - Content-Type: application/pdf, Content-Disposition: attachment
- **Frontend:** `/account/` facturen-tab krijgt "PDF"-knop per row + `downloadFactuurPdf(id, nr)` async function die JWT-Authorization-fetch doet, blob ophaalt, programmatisch downloadt
- **Backups:** `veilingen.js.before-B`, `account-index.html`
- **Verify:**
  - node -c veilingen.js OK
  - pm2 reload OK
  - `/api/factuur/1/pdf` zonder token → 401 ✓
  - Pdfkit sanity-test met fake-data: 2599-byte valid PDF (`%PDF-1.3`, 1 page) — alle blocks renderen
  - Frontend live: "PDF"-knop + function aanwezig in HTML
  - 5/5 publieke routes 200
- **Open:** echte E2E-PDF-test wacht op eerste factuur in productie-DB (nu 0). Endpoint code-validated.
- **Future:** Outfit/Plex Mono font-embed (`doc.registerFont` met TTF), QR-code voor betaal-link (zodra Mollie integreert)

### WD-TICK — Watchdog hourly tick-log ✅ (00:35)
- **Bron:** STABILITEIT-PROTOCOL §B3 — watchdog logt alleen bij fail = "draait niet" vs "alles gezond" ondoorscheidbaar
- **File:** `/opt/t4c/watchdog.sh` (after `echo "0" > "$FAILS_FILE"`)
- **Change:** als `date +%M = 00` → schrijf `[WATCHDOG] tick (alive, server uptime X)` naar log
- **Backup:** `pre-niet-acuut-20260611-0030/watchdog.sh`
- **Verify:** `bash -n` OK, silent-on-healthy intact (run gaf geen log-entry, normaal want minute != 00)

### SMOKE — Smoke-test script + watchdog post-recovery integration ✅ (00:40)
- **Bron:** STABILITEIT-PROTOCOL §B2 + §C3
- **File:** `/opt/t4c/scripts/smoke-test.sh` (nieuw, executable)
- **Checks:** health-endpoint status=ok, 11 publieke routes 200, pm2-error-log scan (recent), DB-integriteit (3 critical tabellen)
- **Exit codes:** 0=OK 1=health 2=routes 3=error-log 4=DB
- **Watchdog integration:** post-recovery roept smoke-test aan; faal → alert in `watchdog-alert.log`
- **Verify:** dry-run exit 0; integratie in watchdog `bash -n` OK

### LOCK-STALE — Lock-file PID-recycling-detectie in server.js ✅ (00:50)
- **Bron:** STABILITEIT-PROTOCOL §B3; PID-recycling kan stale lock geven die nieuwe start blokkeert
- **File:** `/opt/t4c/backend/server.js` `acquireLockOrDie()`
- **Change:** als PID leeft, check ook `/proc/<pid>/cmdline` op `node` + `server.js`. Match = echte t4c-server (refuse). Mismatch = stale lock met PID-recycling (warn + overschrijf).
- **Backup:** `pre-niet-acuut-20260611-0030/server.js`
- **Verify:** node -c OK, pm2 reload schoon (PID 2543434), nieuwe LOCK-warning-text klaar voor recycling-case

### DB-SNAP — Hourly DB-snapshot cron ✅ (00:55)
- **Bron:** STABILITEIT-PROTOCOL §B2; daily backup-cron gaf RPO 24u → te grof voor klant-data
- **File:** `/opt/t4c/scripts/db-snapshot.sh` (nieuw, executable)
- **Werking:** kopieert `t4c.db` naar `/opt/t4c/backups/snapshots/db-YYYYMMDD-HH.db`, retain 24, skip als ongewijzigd of als snapshot in zelfde uur al bestaat. Quick_check vóór copy.
- **Cron:** `15 * * * * /opt/t4c/scripts/db-snapshot.sh` — 1× per uur
- **Verify:** dry-run maakte snapshot 175MB → 183MB op disk; herhalingsrun skipped (idempotent)
- **Impact:** RPO van 24u → 60min voor DB-data
- **Crontab-backup:** `pre-niet-acuut-20260611-0030/crontab.bak`

### RESTORE — Restore-db.sh interactive script ✅ (01:00)
- **Bron:** STABILITEIT-PROTOCOL §C3
- **File:** `/opt/t4c/scripts/restore-db.sh` (nieuw, executable)
- **Werking:** lijst backups + snapshots, user kiest, JA-confirm, pre-restore backup van huidige DB, integrity-check op gekozen file, pm2 stop, lock-cleanup, swap, pm2 start, verify health + telling
- **Exit codes:** 0=OK 1=geen backups/file mist 2=integrity-fail 3=post-restore health-fail
- **Verify:** dry-run lijst toonde 8 backups + nieuwe snapshot; 'q' afsluit normaal

### DUP-PW — /api/me/password verwijderd (was duplicaat) ✅ (01:05)
- **Bron:** AUDIT NICE-TO-HAVE; T4C-MAP-COMPLEET-2026-06-10 §8B
- **Analysis:** Frontend gebruikt enkel `/api/profiel/password` (`account/index.html:396`). Endpoint `/api/me/password` was dead code (legacy).
- **File:** `/opt/t4c/backend/routes/misc.js` (regel 201-213 verwijderd, vervangen door comment-marker)
- **Backup:** `pre-niet-acuut-20260611-0030/misc.js`
- **Verify:** node -c OK, pm2 reload OK; `POST /api/me/password` → 404; `POST /api/profiel/password` → 401 (auth-required, niet 404)

### KLASSIEK — /admin/klassiek mount verwijderd ✅ (01:08)
- **Bron:** T4C-MAP-COMPLEET §10 P2 + memory `feedback_cleanup_test_authflow`
- **Log-check:** geen hits in pm2-logs, geen live nav-refs (alleen `.bak-NavA-20260610` backups noemen 'm)
- **File:** `/opt/t4c/backend/server.js` regel 331-339 verwijderd (replaced met comment)
- **Backup:** zelfde batch (`pre-niet-acuut-20260611-0030/server.js`)
- **Verify:** `/admin/klassiek/` → 404, 6/6 echte admin-pages (`/admin/`, `/inbox/`, `/transport/`, `/analytics/`, `/telex-inkoop/`) → 200
- **Source-dir:** `/opt/t4c/sites/cardatax/admin/` blijft op disk als archive (kan later weg)

### ADDR1 — Schema.org-adres update naar werkelijke vestiging ✅ (00:25)
- **Bron:** user-correctie 2026-06-11 — vestigingsadres is Woudsedijk 11B Langeraar, niet Prins Hendrikstraat 58a Alphen
- **Verificatie (OSM/Nominatim):** Woudsedijk 11B = **Langeraar, postcode 2461 CR**, gemeente Nieuwkoop (Ter Aar is de buurplaats binnen dezelfde gemeente)
- **File:** `/opt/t4c/sites/transfer4cars/index.html` JSON-LD AutoDealer-block (regel 43-45)
- **Backup:** `index.html.bak-ADDR-20260610`
- **Changes:**
  - streetAddress: "Prins Hendrikstraat 58a" → "Woudsedijk 11B"
  - postalCode: "2405 AK" → "2461 CR"
  - addressLocality: "Alphen aan den Rijn" → "Langeraar"
- **Verify:** JSON-LD parsed OK (node parse-test), live URL toont nieuwe adres, geen body-content noemde Alphen
- **Impact:** Google Business / rich-snippets / "near me"-search lokaliseert nu correct op Langeraar
- **Open vervolg:** check of KvK-adres bij JHVT ook bijgewerkt is (geen frontend-actie nodig); cardatax-site separate Schema.org-check

### SLASH1 — Trailing-slash op /privacy + /voorwaarden footer-links ✅ (00:27)
- **Bron:** site-health-scan toonde 301-hop op homepage assets
- **File:** `/opt/t4c/sites/transfer4cars/index.html` regel 508 (footer)
- **Backup:** zelfde batch als ADDR1 (`index.html.bak-ADDR-20260610` — pre-edit state)
- **Changes:** `href="/privacy"` → `href="/privacy/"` + idem voorwaarden
- **Verify:** `curl -sI .../privacy/` → 200 direct, geen redirect-hop meer

### SEC-5 — Disclosure-audit-trail op PUT /api/veiling/:id ✅ (23:55)
- **Bron:** Memory `feedback_cardatax_integrity_guardrail` — anti-fraude, geen km-misleiding/schade-verzwijging
- **File:** `/opt/t4c/backend/routes/veilingen.js` PUT-handler regel 401-410
- **Velden gemonitord:** `km, bouwjaar, beschrijving, highlights, titel, merk, model, minimumprijs`
- **Flow:** vóór UPDATE wordt huidige waarde gelezen, na UPDATE wordt diff bepaald, bij wijziging → `audit_log` entry met action `veiling_disclosure_change` + `details.changes = {field: {from, to}}` + `details.status_change` indien van toepassing
- **Backup:** `pre-quickwins2-20260610-2305/veilingen.js` (zelfde batch als SEC-3)
- **Verify:** node -c OK, pm2 reload OK, 5/5 publieke routes 200
- **Effect:** elke admin/staff die km of beschrijving wijzigt op een veiling laat onveranderbaar audit-spoor achter. Bij betwisting "auto had geen schade" → audit-log toont of beschrijving achteraf is bewerkt.

---

## Herzien (scan-agent zat ernaast op 3 van 3)

### Groep B oorspronkelijk plan ❌ — vervangen
- **FL2 auto-login na `/aanmelden/`** = ONJUIST. `/aanmelden/` is B2B-aanvraag (POST `/api/register` → admin keurt goed → mail met creds). Geen direct account, dus geen auto-login mogelijk. Past wél bij productie-besluit (email+auto-gen-pw via admin-approval). Echte flow = aanvraag→approval→welkomstmail→eerste-login-force-change-pw. **Schaal:** groot, apart traject (zie Groep E hieronder).
- **FL5 contact-knop op `/auto/?id=X`** = AL AANWEZIG (regel 263-270): grote "Bel direct" CTA + contact-block met tel/mail/WhatsApp. Niets te doen.
- **FL20 watchlist-tab op `/account/`** = TE GROOT. Backend leest watchlist (misc.js:118-134), maar er is geen POST/DELETE endpoint én geen heart-knop op /veilingen/ of /auto/. 3 stappen, 3+ files.

## In progress / next

### Groep E (nieuw) — Auth-flow productie-ready
Combineert oud FL2 + welkomstmail-fix + productie-besluit (email+auto-gen-pw, 2FA TOTP, dashboard pw-change).
- Stap 1: admin keurt aanvraag goed → server genereert pw → mail (vereist SMTP-fix SEC-3)
- Stap 2: klant logt in met mail+pw → eerste-login force-change-pw modal in `/account/`
- Stap 3: 2FA TOTP setup-flow in `/account/` (optioneel inschakelbaar, mail-code-fallback)
- Stap 4: Google-login spoor (`MORGEN-2026-06-11.md`) schrappen
- Apart traject, eerst design + SMTP-besluit met user

### Groep C — Ontbrekende admin-UIs (FL6/FL7/FL8/FL10)
- `/admin/voorraad/`, `/admin/inkoop/`, `/admin/taxaties/`, `/admin/users/`
- Lege-state + read-only eerst, CRUD later

### Groep D — Betaal-flow (FL1 + FL3 + FL4)
- **Kritieke launch-blocker.** Factuur-detail-page + iDEAL/Mollie/Stripe-knop + `facturen`-tabel vullen + admin-factuur-CRUD
- Apart traject, eerst design-overleg met user

---

## Backlog / beslis-punten

- "CarDatax" link in T4C-admin-nav — verwijderen (groep A) of behouden? Default: **verwijderen** (PWA-bookmark apart)
- `/verkoop/aanbod/` + `/verkoop/auto/` (legacy dubbel) — verwijderen of doel verhelderen?
- `/admin/klassiek/` static dir behouden of mappen-verplaatsen naar `/opt/t4c/backups/`?
- 10× `.pre-dna-*` backup-files in transfer4cars-tree — wanneer naar `/opt/t4c/backups/cleanup-20260610/`?
- Welkomstmail SMTP 451 (provider-side) — wanneer fixen?
- `facturen` + `biedingen` lege tabellen (code refereert): vullen of opruimen?
